"use client";
import ProtectedRoute from "./ProtectedRoute";
import { useTranslations } from 'next-intl';

const App = () => {
  const t = useTranslations(); // This works on the client-side now

  return (
    <ProtectedRoute>
      <div>
        <h1>{t('welcomeMessage')}</h1> {/* Example translation key */}
      </div>
    </ProtectedRoute>
  );
};

export default App;
